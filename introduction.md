# Introduction

Types of Vectors:

* **Sparse Vectors**: A sparse vector is a high-dimensional vector where only a small number of elements have non-zero values.
* **Dense Vectors**: A dense vector has fewer dimensions, with most or all elements being non-zero.



* To perform a semantic search in Elasticsearch, you need to use the semantic query type on data stored in a `semantic_text` field
* To view semantic data in the response, you can use `highlight` to view relevant text segments for each search hit in the response.
* To perform a semantic search in Elasticsearch, you need to use the semantic query type on data stored in a `semantic_text` field
* To view semantic data in the response, you can use `highlight` to view relevant text segments for each search hit in the response.



